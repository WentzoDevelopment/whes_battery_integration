{
  "config": {
    "step": { "user": { "title": "Configure WHES", "description": "Enter API credentials and IDs" } },
    "error": { "cannot_connect": "Failed to connect to WHES API", "invalid_auth": "Invalid credentials or IDs" },
    "options": { "step": { "init": { "title": "WHES options" } } }
  }
}
